{
  "name": "creamy-cravings",
  "version": "1.0.0",
  "description": "Creamy Cravings - Cheesecake Works - A luxury dessert e-commerce experience",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "start": "npm start --prefix server",
    "start:server": "node server/index.js",
    "seed": "node server/seed.js",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "build:render": "npm install --prefix server && npm install --prefix client --include=dev && npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
